# FRIS
Finite-horizon Information-State Communication Scheduling (FICS)

Run the paper experiments:
`python run_experiments.py`

Optional (also generate the trace-not-enough counterexample figure):
`python run_experiments.py --with-counterexample`
